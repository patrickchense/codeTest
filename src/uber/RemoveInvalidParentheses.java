package uber;

/*
given a string as input, which only contains left and right parentheses.
remove the MINIMUM number of characters in the string to make the parentheses valid.
example:
(() -> ()
(())()) -> (()()) or (())() are both ok, which removes only 1 ')' to make the string valid
((( -> "", return empty string
))) -> ""

@phone

 */
public class RemoveInvalidParentheses {
}
